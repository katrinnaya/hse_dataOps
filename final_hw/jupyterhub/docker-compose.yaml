version: '3.8'
services:
  jupyterhub:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./jupyterhub_config.py:/root/jupyterhub_config.py
      - jupyter_data:/root/jupyterhub
    environment:
      JUPYTERHUB_ADMIN: ${JUPYTERHUB_ADMIN}

volumes:
  jupyter_data:
